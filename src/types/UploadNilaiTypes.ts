/** @format */

import JadwalTypes from "./JadwalTypes";

interface UploadNilaiTypes {
  id: number | string;
  jadwal_id: number | string;
  file: string;
  jadwal: JadwalTypes;
}
export default UploadNilaiTypes;
